% !TeX encoding = UTF-8
% !TeX spellcheck = en_GB
% !TeX root = ../thesis.tex

\chapter{Documentation}
\label{chapter:documentation}

Node.js and NPM 

To build a project, first i installed node.js and npm.
I use Parcel for bundeling.

npm install

To install Parcel:
npm install --save-dev parcel


https://grafana.com/
https://prometheus.io/
https://www.youtube.com/watch?v=h4Sl21AKiDg

added to autostart in OpenWrt for OVS
/usr/share/openvswitch/scripts/ovs-ctl start



Python server:
pip install Flask, flask-cors
to ensure the server is running, i added it to the autostart
echo -e "$(sudo cat /etc/rc.local | grep -v 'exit 0')\npython3 /config/www/community/visualization/src/python/server.py &\nexit 0" | sudo tee /etc/rc.local && sudo chmod +x /etc/rc.local

In addition in the Node.js environment i use child_process to keep the server running but this wont work in the real environment.
